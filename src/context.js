import { createContext } from 'react';

const storeContext = createContext(null);
const modalContext = createContext(null);
const uploaderContext = createContext(null);

export { storeContext, modalContext, uploaderContext };
